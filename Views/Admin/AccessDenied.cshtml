@{
    ViewData["Title"] = "Access denied";
    Layout = "_AdminLayout";
}

<div class="glass-card p-5 text-center">
    <i class="bi bi-shield-lock display-4 text-warning d-block mb-3"></i>
    <h4>Access denied</h4>
    <p class="text-muted mb-4">You don’t have permission for this menu. Ask a SuperAdmin to allot it under <strong>System → Admin Users</strong>.</p>
    <a asp-action="Index" class="btn btn-crm-primary">Back to Dashboard</a>
</div>
